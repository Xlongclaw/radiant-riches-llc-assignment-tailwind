import NavigationBar from "@/containers/navigation-bar";
import React from "react";

const page: React.FC = () => {
  return (
    <div>
      <NavigationBar />
    </div>
  );
};

export default page;
